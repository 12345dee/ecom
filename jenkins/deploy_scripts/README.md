Deployment scripts:
- ecs_deploy_bluegreen.sh : performs blue/green deployment using AWS CLI + jq
Make sure the host running the script has aws CLI and jq installed and credentials (or Jenkins supplies creds).
